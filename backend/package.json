{
  "version": "0.0.1",
  "name": "rabbitmq-chat-backend",
  "description": "web server and consumer of websocket and rabbitmq",
  "scripts": {
    "server": "node server",
    "consumer": "node consumer",
    "dockercmd": "npm run server & npm run consumer"
  },
  "dependencies": {
    "amqplib": "^0.8.0",
    "socket.io": "^4.4.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
